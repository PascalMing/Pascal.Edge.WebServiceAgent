# Pascal.Edge.WebServiceAgent

Pascal.Edge.WebServiceAgent is a .NET library that provides a simple way to interact with web services. It allows you to easily consume web services using SOAP, REST, and other protocols.
## Features
- 提供WebService静态文件访问服务和动态代理服务，支持跨域访问。
- 为Vue的Dist目录提供静态文件服务，方便前端集成。